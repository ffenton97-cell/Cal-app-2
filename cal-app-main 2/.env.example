# Anthropic — server-only; set in Vercel Project → Settings → Environment Variables
ANTHROPIC_API_KEY=

# Optional model override (server API routes use CLAUDE_MODEL; client uses NEXT_PUBLIC_*)
CLAUDE_MODEL=
NEXT_PUBLIC_CLAUDE_MODEL=
